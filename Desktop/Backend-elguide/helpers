# 🚀 Commandes Git essentielles

## 🔹 Configuration de Git
```bash
git config --global user.name "Ton Nom"
git config --global user.email "tonemail@example.com"
```

## 🔹 Initialisation d’un dépôt Git
```bash
git init
```

## 🔹 Cloner un dépôt existant
```bash
git clone https://github.com/utilisateur/nom-du-repo.git
```

## 🔹 Ajout et validation des fichiers
```bash
git add .    # Ajoute tous les fichiers
git commit -m "Message clair sur la modification"
```

## 🔹 Envoi des modifications vers GitHub
```bash
git push origin main
```

## 🔹 Mettre à jour son dépôt local
```bash
git pull origin main
```

## 🔹 Changement de branche
```bash
git branch     # Affiche toutes les branches
git checkout nom_de_la_branche   # Change de branche
git checkout -b nouvelle_branche  # Crée une nouvelle branche et bascule dessus
```

## 🔹 Gestion des conflits
```bash
git status
git add fichier_conflit.py
git commit -m "Résolution des conflits"
git push origin main
```

## 🔹 Annuler ou récupérer un état précédent
```bash
git reset --hard HEAD~1   # Annule le dernier commit
git reset --soft HEAD~1   # Annule le dernier commit mais garde les fichiers modifiés
git stash                 # Met de côté les modifications non validées
git stash pop             # Récupère les modifications mises de côté
```

---

# 📂 Le fichier `.gitignore`
Ce fichier sert à **ignorer certains fichiers ou dossiers** pour qu'ils ne soient pas suivis par Git.

### Exemple de `.gitignore` pour un projet Django :
```
# Dossiers de l'environnement virtuel
env/
venv/

# Fichiers de configuration
*.log
*.sqlite3
__pycache__/
db.sqlite3
*.pyc

# Clés secrètes et fichiers sensibles
.env
*.pem
```

---

# 🛠 L’environnement virtuel (`env`)
Un **environnement virtuel** permet d’installer des dépendances Python **sans impacter le système global**.

## 🔹 Créer un environnement virtuel
```bash
python -m venv env
```

## 🔹 Activer l’environnement
- **Windows (PowerShell)**  
  ```powershell
  env\Scripts\Activate
  ```
- **Linux/macOS**  
  ```bash
  source env/bin/activate
  ```

## 🔹 Installer les dépendances du projet
```bash
pip install -r requirements.txt
```

## 🔹 Désactiver l’environnement virtuel
```bash
deactivate
```

